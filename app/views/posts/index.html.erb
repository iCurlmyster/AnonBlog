
<%= render 'sidebar' %>

<div class="content">

<table class="table table-hover" id="index_table">
 <thead>
    <tr>
      <th class="text-center" id="text">Title</th>
      
    </tr>
    </thead>

  
    <% @posts.each do |post| %>
      <tr>
        <td class="text-center"><%= link_to post.title, post %></td>
       <!-- <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  
</table>

</div>